{
  "version": 1,
  "name": "sample-openapi",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "verify": "pnpm typecheck && pnpm test",
    "dev": "pnpm --silent test"
  },
  "keywords": [
    "profile",
    "compiler",
    "integration",
    "catalog"
  ],
  "_kilo_change_markers": [
    "forced-profile-4:test",
    "forced-catalog-6:test"
  ]
}
