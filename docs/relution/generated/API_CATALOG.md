# Relution API operation catalog

Status: **not generated**.

This commit-safe placeholder is intentionally non-runnable. Generate a
target-local catalog from the exact authorized instance OpenAPI export and
validate it with `--spec` before using any operation. Keep customer contracts,
hostnames, and generated operation catalogs out of the repository by default.
